package TECHNOSTUDY_USA.day99.Replit.ReplitCollections;

import java.util.HashSet;

public class Set1 {
    /*
    Create a method totalCount()
parameter is integer Hashset
 Get the number of elements in the hash set
return the totalCount
Example:
hashset ; 4,2,3,1,7
result should be 5
     */
    public static int totalCount(HashSet<Integer> list) {
        //int totalCount = 0;
 //totalCount = list.size();
        // return totalCount;
        return list.size();
    }
}
